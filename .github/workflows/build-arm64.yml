name: Build for Raspberry Pi (ARM64)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-arm64:
    runs-on: ubuntu-24.04  # 使用 24.04 以获取较新的库 (Webkit 4.1)
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl wget git build-essential pkg-config libssl-dev
          
          # 1. Setup Multiarch for ARM64
          sudo dpkg --add-architecture arm64
          sudo apt-get update
          
          # 2. Install Cross-Compiler and Target Libraries
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
          sudo apt-get install -y libwebkit2gtk-4.1-dev:arm64 libgtk-3-dev:arm64 libayatana-appindicator3-dev:arm64 librsvg2-dev:arm64 libssl-dev:arm64

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install NPM Dependencies
        run: npm install

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Configure Environment for Cross-Compilation
        run: |
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig" >> $GITHUB_ENV
          echo "PKG_CONFIG_ALLOW_CROSS=1" >> $GITHUB_ENV

      - name: Build Tauri App
        run: npm run tauri build -- --target aarch64-unknown-linux-gnu

      - name: Upload Deb Artifact
        uses: actions/upload-artifact@v4
        with:
          name: monitor-dashboard-deb-arm64
          path: src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/deb/*.deb

      - name: Upload AppImage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: monitor-dashboard-appimage-arm64
          path: src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/appimage/*.AppImage
